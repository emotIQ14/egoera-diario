# Egoera App — variables de entorno (ejemplo). Copia a .env.local y rellena.

# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo servidor / migraciones (NO exponer al cliente)
SUPABASE_SERVICE_ROLE_KEY=
